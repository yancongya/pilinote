# Backend binaries

Put packaged API binaries here before running desktop package build:

- `mac/pilinote-api-macos`
- `win/pilinote-api.exe`
- optional `linux/pilinote-api`

These files are copied into Electron `resources/backend/*` during packaging.
